services:
  - type: web
    name: steps-web
    runtime: python
    buildCommand: python -m pip install "fastapi>=0.135.1" "uvicorn>=0.41.0"
    startCommand: PYTHONPATH=src:steps-web/backend python -m uvicorn steps_web_backend.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health